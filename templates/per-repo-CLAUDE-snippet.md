## Commit messages

Use Conventional Commits: `type(scope): subject`, with a subject of at most
72 characters and no trailing period. A `Why:` body line is required only for
commits of type `feat` changing more than 50 lines; otherwise the body is optional.
Repository CI defines the enforced policy.
